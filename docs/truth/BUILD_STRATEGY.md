# Build Strategy

Defines how Nexus0.5 is built and validated locally.

## Initial Strategy
- Autobuilder is the mission-driven build engine.
- Build from clean target structure, not inherited legacy trees.
- Validate incrementally with reproducible local checks.
- Promote only validated artifacts toward release packaging.
